services:
  languagetool:
    networks:
      # Add external networks here as needed
      - cloudflared-net       # For Cloudflare tunnel
      # - nginx-reverse-proxy   # For nginx-proxy with SSL
      # - caddy-net             # For Caddy proxy
    
    # Uncomment if using nginx-proxy with SSL
    # environment:
    #   VIRTUAL_HOST: ${LANGUAGETOOL_DOMAIN:-languagetool.example.com}
    #   VIRTUAL_PORT: 8010
    #   LETSENCRYPT_HOST: ${LANGUAGETOOL_DOMAIN:-languagetool.example.com}
    #   LETSENCRYPT_EMAIL: ${LETSENCRYPT_EMAIL}
    
    # Expose port locally for testing (not recommended in production)
    # ports:
    #   - "8010:8010"
networks:
  cloudflared-net:
    external: true
